diff --git a/src/interpreter_v2/economy_phases.zig b/src/interpreter_v2/economy_phases.zig
index b78cbb1..7902c9b 100644
--- a/src/interpreter_v2/economy_phases.zig
+++ b/src/interpreter_v2/economy_phases.zig
@@ -26,6 +26,7 @@ const Measurement = struct {
     peak_working_bytes: usize,
     output_bytes: usize,
     output_sha256: [64]u8,
+    unmaterialized_view_observations: usize,
 };
 
 fn measure(init: std.process.Init, program: data.program.Program, initial: []const u8, storage: []u8) !Measurement {
@@ -39,12 +40,17 @@ fn measure(init: std.process.Init, program: data.program.Program, initial: []con
     try machine.initialize(initial);
     phases.initialization_ns = elapsed(init.io, start);
     var outcome: process.Outcome = undefined;
+    var view_observations: usize = 0;
     for (0..1000000) |_| {
         const before = statistics;
         const unwinding = machine.status == .unwinding;
         start = std.Io.Clock.awake.now(init.io);
         const terminal = try machine.step();
         const duration = elapsed(init.io, start);
+        var views = machine.store.views.valueIterator();
+        while (views.next()) |view| if (!view.materialized) {
+            view_observations += 1;
+        };
         if (statistics.direct_clauses != before.direct_clauses) {
             phases.direct_clause_ns += duration;
         } else if (statistics.one_shot_captures != before.one_shot_captures) {
@@ -76,7 +82,7 @@ fn measure(init: std.process.Init, program: data.program.Program, initial: []con
     defer init.gpa.free(bytes);
     _ = try data.protocol.encode(data.protocol.Outcome, allocator, outcome.record, bytes);
     phases.outcome_encoding_ns = elapsed(init.io, start);
-    return .{ .phases = phases, .statistics = statistics, .peak_working_bytes = arena.peak_payload, .output_bytes = bytes.len, .output_sha256 = std.fmt.bytesToHex(data.wire.digest(bytes), .lower) };
+    return .{ .phases = phases, .statistics = statistics, .peak_working_bytes = arena.peak_payload, .output_bytes = bytes.len, .output_sha256 = std.fmt.bytesToHex(data.wire.digest(bytes), .lower), .unmaterialized_view_observations = view_observations };
 }
 
 pub fn main(init: std.process.Init) !void {
diff --git a/src/interpreter_v2/economy_tests.zig b/src/interpreter_v2/economy_tests.zig
index 16cc374..d37c328 100644
--- a/src/interpreter_v2/economy_tests.zig
+++ b/src/interpreter_v2/economy_tests.zig
@@ -92,7 +92,7 @@ test "collection and canonicalization traverse each reachable node and edge once
     try std.testing.expectEqual(@as(u64, 5), statistics.traced_edges);
     try std.testing.expectEqual(@as(u64, 3), statistics.swept_slots);
     var measured: data.snapshot.Statistics = .{};
-    var normalized = try data.snapshot.canonicalizeMeasured(allocator, store.state(@splat(0), .active, roots), &measured);
+    var normalized = try data.snapshot.canonicalizeMeasured(allocator, try store.state(@splat(0), .active, roots), &measured);
     defer normalized.deinit();
     try std.testing.expectEqual(statistics.traced_nodes, measured.nodes);
     try std.testing.expectEqual(statistics.traced_edges, measured.edges);
@@ -165,3 +165,56 @@ fn ownedExitReplacementCase(a: std.mem.Allocator) !void {
 test "owned exit replacement retains nested cancellation bytes and both field buffers" {
     try std.testing.checkAllAllocationFailures(allocator, ownedExitReplacementCase, .{});
 }
+
+fn continuationViewCase(a: std.mem.Allocator) !void {
+    var store: Store = .{ .allocator = a };
+    defer store.deinit();
+    const program: data.program.Program = .{ .roots = .{ .entry = 0, .result = 0, .failure = 0 }, .schemas = &.{.bytes}, .constants = &.{}, .effects = &.{}, .functions = &.{}, .blocks = &.{} };
+    const dead = blk: {
+        const bytes = try a.alloc(u8, 65539);
+        defer a.free(bytes);
+        @memset(bytes, 0x5a);
+        @memcpy(bytes[0..3], &[_]u8{ 0x80, 0x80, 0x04 });
+        break :blk try store.literal(program, .{ .schema = 0, .bytes = bytes });
+    };
+    const scalar: g.Value = .{ .schema = 1, .body = .{ .scalar = @splat(7) } };
+    const ref = blk: {
+        const backing = try a.alloc(g.Value, 4);
+        errdefer a.free(backing);
+        @memset(backing, scalar);
+        backing[3] = dead;
+        break :blk (try store.tryContinuationView(.{
+            .source_block = 0,
+            .arguments = &.{},
+            .parent = null,
+            .evidence = null,
+        }, backing, &.{ .{ .slot = 2 }, .returned, .{ .slot = 2 }, .{ .slot = 0 } })) orelse return error.OutOfMemory;
+    };
+    const roots: g.Roots = .{ .current = ref };
+    try store.collect(roots);
+    try std.testing.expect(!store.blob_alive.items[@intCast(dead.body.blob.id)]);
+    const result: g.Value = .{ .schema = 1, .body = .{ .scalar = @splat(42) } };
+    const resumed = try store.resumedControl(ref, result);
+    defer a.free(resumed.arguments);
+    try std.testing.expectEqualSlices(g.Value, &.{ scalar, result, scalar, scalar }, resumed.arguments);
+    const logical = (try store.get(ref)).continuation;
+    try std.testing.expectEqualSlices(?g.Value, &.{ scalar, null, scalar, scalar }, logical.arguments);
+    var output: [2048]u8 = undefined;
+    const bytes = try data.snapshot.encode(a, try store.state(@splat(0), .active, roots), &output);
+    var expected: [2048]u8 = undefined;
+    const original: g.State = .{ .program_identity = @splat(0), .status = .active, .roots = .{ .current = .{ .id = 0 } }, .nodes = &.{.{ .continuation = .{
+        .source_block = 0,
+        .arguments = &.{ scalar, null, scalar, scalar },
+        .parent = null,
+        .evidence = null,
+    } }} };
+    try std.testing.expectEqualSlices(u8, try data.snapshot.encode(a, original, &expected), bytes);
+    try store.collect(roots);
+    const again = try store.resumedControl(ref, result);
+    defer a.free(again.arguments);
+    try std.testing.expectEqualSlices(g.Value, resumed.arguments, again.arguments);
+}
+
+test "continuation views preserve holes and permutations without retaining dead roots" {
+    try std.testing.checkAllAllocationFailures(allocator, continuationViewCase, .{});
+}
diff --git a/src/interpreter_v2/machine.zig b/src/interpreter_v2/machine.zig
index f5846b0..20b7e98 100644
--- a/src/interpreter_v2/machine.zig
+++ b/src/interpreter_v2/machine.zig
@@ -15,6 +15,8 @@ pub const Machine = struct {
     roots: g.Roots = .{},
     status: g.Status = .active,
     statistics: ?*process.Statistics = null,
+    active_slots: ?[]g.Value = null,
+    slots_taken: bool = false,
 
     pub fn initialize(self: *Machine, initial: []const u8) Error!void {
         var temporary = std.heap.ArenaAllocator.init(self.allocator);
@@ -48,6 +50,23 @@ pub const Machine = struct {
 
     pub fn continuation(self: *Machine, source: p.Id, slots: []const g.Value, control: g.Control) Error!g.NodeRef {
         const edge = data.state_admission.next(self.program.blocks[@intCast(source)].terminator).?;
+        // Amortize the private view index, and bound unused descriptor retention.
+        if (!self.slots_taken and edge.arguments.len >= 16 and
+            slots.len -| edge.arguments.len <= edge.arguments.len)
+        {
+            if (self.active_slots) |backing| if (backing.ptr == slots.ptr and backing.len == slots.len) {
+                if (try self.store.tryContinuationView(.{
+                    .source_block = source,
+                    .arguments = &.{},
+                    .parent = control.parent,
+                    .evidence = control.evidence,
+                    .region = control.region,
+                }, backing, edge.arguments)) |ref| {
+                    self.slots_taken = true;
+                    return ref;
+                }
+            };
+        }
         const arguments = try self.allocator.alloc(?g.Value, edge.arguments.len);
         errdefer self.allocator.free(arguments);
         for (arguments, edge.arguments) |*argument, spec| argument.* = switch (spec) {
@@ -58,19 +77,12 @@ pub const Machine = struct {
     }
 
     pub fn resumeContinuation(self: *Machine, reference: g.NodeRef, value: g.Value) Error!void {
-        const saved = (try self.store.get(reference)).continuation;
-        const edge = data.state_admission.next(self.program.blocks[@intCast(saved.source_block)].terminator).?;
-        const arguments = try self.allocator.alloc(g.Value, saved.arguments.len);
-        errdefer self.allocator.free(arguments);
-        for (arguments, saved.arguments) |*argument, item| argument.* = item orelse value;
-        self.roots.current = try self.store.addOwned(.{ .control = .{
-            .block = edge.block,
-            .arguments = arguments,
-            .parent = saved.parent,
-            .evidence = saved.evidence,
-            .region = saved.region,
-        } });
-        self.roots.evidence = saved.evidence;
+        var control = try self.store.resumedControl(reference, value);
+        errdefer self.allocator.free(control.arguments);
+        const edge = data.state_admission.next(self.program.blocks[@intCast(control.block)].terminator).?;
+        control.block = edge.block;
+        self.roots.current = try self.store.addOwned(.{ .control = control });
+        self.roots.evidence = control.evidence;
     }
 
     fn jump(self: *Machine, edge: p.Edge, slots: []const g.Value, control: g.Control) Error!void {
@@ -203,7 +215,13 @@ pub const Machine = struct {
         const control = (try self.store.get(self.roots.current.?)).control;
         const block = self.program.blocks[@intCast(control.block)];
         const slots = (try self.evaluateBlock(block, control.arguments, control)) orelse return null;
-        defer self.allocator.free(slots);
+        self.active_slots = slots;
+        self.slots_taken = false;
+        defer {
+            self.active_slots = null;
+            if (!self.slots_taken) self.allocator.free(slots);
+            self.slots_taken = false;
+        }
         switch (block.terminator) {
             .return_value => |slot| {
                 return self.returnTo(control.parent, slots[@intCast(slot)]);
@@ -422,6 +440,10 @@ pub const Machine = struct {
     pub fn returnTo(self: *Machine, parent: ?g.NodeRef, value: g.Value) Error!?Outcome {
         var cursor = parent;
         while (cursor) |ref| {
+            if (try self.store.kind(ref) == .continuation) {
+                try self.resumeContinuation(ref, value);
+                return null;
+            }
             const frame = try self.store.get(ref);
             // Closing empty lexical region wrappers is a finite graph walk.
             // It never executes a second authored block or recurses in Zig.
@@ -608,7 +630,7 @@ pub const Machine = struct {
         errdefer arena.deinit();
         const output = arena.allocator();
         const measurement = if (self.statistics) |s| &s.snapshot else null;
-        var emission = try data.snapshot.emit(self.allocator, self.store.state(self.identity, self.status, self.roots), output, measurement);
+        var emission = try data.snapshot.emit(self.allocator, try self.store.state(self.identity, self.status, self.roots), output, measurement);
         defer emission.normalized.deinit();
         const normalized = emission.normalized;
         try data.state_admission.validate(self.allocator, self.program, normalized.state);
diff --git a/src/interpreter_v2/store.zig b/src/interpreter_v2/store.zig
index a7cbc19..e37cd76 100644
--- a/src/interpreter_v2/store.zig
+++ b/src/interpreter_v2/store.zig
@@ -5,6 +5,18 @@ const data = @import("boundary_data_v2");
 const g = data.graph;
 pub const Error = data.snapshot.Error;
 const empty: g.Node = .{ .environment = .{ .values = &.{}, .tail = null } };
+const ContinuationView = struct {
+    backing: []g.Value,
+    arguments: []const data.program.Argument,
+    materialized: bool = false,
+
+    fn argument(self: ContinuationView, index: usize) ?g.Value {
+        return switch (self.arguments[index]) {
+            .slot => |slot| self.backing[@intCast(slot)],
+            .returned => null,
+        };
+    }
+};
 
 pub const Statistics = struct {
     added_nodes: u64 = 0,
@@ -27,9 +39,10 @@ pub const Store = struct {
     marks: std.ArrayList(bool) = .empty,
     blob_marks: std.ArrayList(bool) = .empty,
     pending: std.ArrayList(data.snapshot.Reference) = .empty,
+    views: std.AutoHashMapUnmanaged(u64, ContinuationView) = .empty,
 
     pub fn deinit(self: *Store) void {
-        for (self.nodes.items, self.alive.items) |item, live| if (live) release(g.Node, self.allocator, item);
+        for (self.nodes.items, self.alive.items, 0..) |item, live, id| if (live) self.releaseNode(id, item);
         for (self.blobs.items, self.blob_alive.items) |blob, live| if (live) self.allocator.free(blob.bytes);
         self.nodes.deinit(self.allocator);
         self.alive.deinit(self.allocator);
@@ -41,6 +54,7 @@ pub const Store = struct {
         self.marks.deinit(self.allocator);
         self.blob_marks.deinit(self.allocator);
         self.pending.deinit(self.allocator);
+        self.views.deinit(self.allocator);
         self.* = undefined;
     }
 
@@ -68,16 +82,69 @@ pub const Store = struct {
         return .{ .id = id };
     }
 
-    pub fn get(self: Store, reference: g.NodeRef) Error!g.Node {
+    fn raw(self: Store, reference: g.NodeRef) Error!g.Node {
         if (reference.id >= self.nodes.items.len or !self.alive.items[@intCast(reference.id)]) return error.InvalidReference;
         return self.nodes.items[@intCast(reference.id)];
     }
 
+    pub fn kind(self: Store, reference: g.NodeRef) Error!g.NodeTag {
+        return std.meta.activeTag(try self.raw(reference));
+    }
+
+    pub fn get(self: *Store, reference: g.NodeRef) Error!g.Node {
+        _ = try self.raw(reference);
+        if (self.views.getPtr(reference.id)) |view| if (!view.materialized) {
+            const arguments = try self.allocator.alloc(?g.Value, view.arguments.len);
+            for (arguments, 0..) |*argument, index| argument.* = view.argument(index);
+            self.nodes.items[@intCast(reference.id)].continuation.arguments = arguments;
+            // Keep the old backing until collection: the capturing step may
+            // still borrow its evaluated slots after this materialization.
+            view.materialized = true;
+        };
+        return self.nodes.items[@intCast(reference.id)];
+    }
+
+    /// Successful insertion takes the immutable evaluated buffer. A null result
+    /// leaves it with the caller, which can use ordinary materialization.
+    pub fn tryContinuationView(
+        self: *Store,
+        saved: g.Continuation,
+        backing: []g.Value,
+        arguments: []const data.program.Argument,
+    ) Error!?g.NodeRef {
+        self.views.ensureUnusedCapacity(self.allocator, 1) catch return null;
+        const ref = self.addOwned(.{ .continuation = saved }) catch {
+            if (self.views.count() == 0) {
+                self.views.deinit(self.allocator);
+                self.views = .empty;
+            }
+            return null;
+        };
+        self.views.putAssumeCapacity(ref.id, .{ .backing = backing, .arguments = arguments });
+        return ref;
+    }
+
+    pub fn resumedControl(self: *Store, reference: g.NodeRef, value: g.Value) Error!g.Control {
+        const saved = (try self.raw(reference)).continuation;
+        const view = self.views.get(reference.id);
+        const count = if (view) |v| v.arguments.len else saved.arguments.len;
+        const arguments = try self.allocator.alloc(g.Value, count);
+        for (arguments, 0..) |*argument, index| {
+            argument.* = (if (view) |v| v.argument(index) else saved.arguments[index]) orelse value;
+        }
+        return .{ .block = saved.source_block, .arguments = arguments, .parent = saved.parent, .evidence = saved.evidence, .region = saved.region };
+    }
+
+    fn releaseNode(self: *Store, id: u64, node: g.Node) void {
+        if (self.views.fetchRemove(id)) |entry| self.allocator.free(entry.value.backing);
+        release(g.Node, self.allocator, node);
+    }
+
     pub fn replace(self: *Store, reference: g.NodeRef, value: g.Node) Error!void {
         const previous = try self.get(reference);
         const replacement = try duplicate(g.Node, self.allocator, value);
         self.nodes.items[@intCast(reference.id)] = replacement;
-        release(g.Node, self.allocator, previous);
+        self.releaseNode(reference.id, previous);
     }
 
     /// Takes independently allocated record slices on success. No incoming
@@ -85,7 +152,7 @@ pub const Store = struct {
     pub fn replaceOwned(self: *Store, reference: g.NodeRef, value: g.Node) Error!void {
         const previous = try self.get(reference);
         self.nodes.items[@intCast(reference.id)] = value;
-        release(g.Node, self.allocator, previous);
+        self.releaseNode(reference.id, previous);
     }
 
     pub fn literal(self: *Store, program: data.program.Program, value: data.program.Literal) Error!g.Value {
@@ -132,10 +199,28 @@ pub const Store = struct {
         }
     }
 
-    pub fn state(self: Store, identity: [32]u8, status: g.Status, roots: g.Roots) g.State {
+    pub fn state(self: *Store, identity: [32]u8, status: g.Status, roots: g.Roots) Error!g.State {
+        for (self.alive.items, 0..) |live, id| if (live) {
+            _ = try self.get(.{ .id = id });
+        };
         return .{ .program_identity = identity, .status = status, .roots = roots, .nodes = self.nodes.items, .blobs = self.blobs.items };
     }
 
+    fn traceNode(self: *Store, id: u64, pending: *std.ArrayList(data.snapshot.Reference)) Error!void {
+        if (self.views.get(id)) |view| {
+            if (view.materialized) {
+                self.allocator.free(view.backing);
+                _ = self.views.remove(id);
+            } else {
+                for (0..view.arguments.len) |index| {
+                    if (view.argument(index)) |value|
+                        try data.snapshot.references(g.Value, value, pending, self.allocator);
+                }
+            }
+        }
+        try data.snapshot.references(g.Node, self.nodes.items[@intCast(id)], pending, self.allocator);
+    }
+
     /// Traces strong reachability, including cycles. Reclamation performs no effects.
     pub fn collect(self: *Store, roots: g.Roots) Error!void {
         try self.marks.ensureTotalCapacityPrecise(self.allocator, self.nodes.items.len);
@@ -156,7 +241,7 @@ pub const Store = struct {
                 if (marks[@intCast(id)]) continue;
                 marks[@intCast(id)] = true;
                 const before = pending.items.len;
-                try data.snapshot.references(g.Node, self.nodes.items[@intCast(id)], pending, self.allocator);
+                try self.traceNode(id, pending);
                 if (self.statistics) |s| {
                     s.traced_nodes +|= 1;
                     s.traced_edges +|= pending.items.len - before;
@@ -171,7 +256,7 @@ pub const Store = struct {
         try self.free_blobs.ensureTotalCapacity(self.allocator, blob_marks.len);
         if (self.statistics) |s| s.swept_slots +|= marks.len + blob_marks.len;
         for (marks, 0..) |marked, id| if (!marked and self.alive.items[id]) {
-            release(g.Node, self.allocator, self.nodes.items[id]);
+            self.releaseNode(id, self.nodes.items[id]);
             self.nodes.items[id] = empty;
             self.alive.items[id] = false;
             self.free_nodes.appendAssumeCapacity(id);
