# Dynamic subagents

Statically selected providers are compiled into the parent application. An
independently deployed child instead crosses the residual effect boundary as
`agent.invoke.v1`.

The receiver-selected capability validates the child artifact and local
policy, drives it through a compatible world-host, and returns only a bounded
result. It cannot author the parent Frame or import receiver authority into the
application.
