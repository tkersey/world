# Cleanup regression inputs

These four immutable BPI2/source vectors were generated by Boundary's public
builder at `c914de195a6003076629e650ba0b6496d5fcdb75`. Their expectations come from
that revision's independent source oracle, whose digest is recorded in
`expectations.json`. The owned-result case independently requires the ordered
writer log `[3, 7, 99]`: outer finalizer, disposal of the protected generator,
then the aborting clause's marker.

World owns these regression inputs because its pinned Boundary release predates
their fixture generator and the corresponding oracle correction. All existing
compiler-generated fixtures continue to use the caller-selected source oracle.
These supplemental vectors always run, even when the caller uses the pinned
release. They are conformance data, not an alternative evaluator or runtime API.

The supplied oracle is still executed. Its exact pinned-release digest selects
the recorded predecessor results: that oracle loses a primary failure during
cleanup and omits the protected generator's finalizer. Other oracle versions
must agree with the corrected expectations. Runtime results always use the
corrected expectations; matching the predecessor oracle never excuses a kernel
failure or omitted finalizer.

To regenerate, emit Boundary's source fixtures, evaluate the four cleanup source
JSON files with its corrected `test/v2/source_oracle.mjs`, and compare the BPI2,
source and expected bytes before replacing these files. Preserve the provenance
and the full run/advance/cancellation checks in `source_transfer.mjs`.
